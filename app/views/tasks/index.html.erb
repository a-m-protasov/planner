<h1>Planner</h1>  
<ul>
<% @all_tasks.each do |task| %>
 <li><%=link_to('edit', "/edit/#{task.id}") + ' ' + link_to('del', "/delete/#{task.id}") + ' ' + task.title + " " + task.deadline.mday.to_s + '/' + task.deadline.mon.to_s + '/' + task.deadline.year.to_s %></li> 
<% end %>

</ul>

<%= link_to('create', '/new')%>
